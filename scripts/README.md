# Scripts

Helper scripts used from Kaggle notebooks (or copied into notebook cells).

## Planned scripts

| File | Purpose |
| --- | --- |
| `build_charset.py` | Build Bangla + English character dictionary from labels |
| `prepare_recognition_data.py` | Crop / assemble recognition label files |
| `validate_detection_data.py` | Check detection image paths + polygon format |
| `validate_recognition_data.py` | Check recognition crops + label files |
| `evaluate_recognition.py` | CER / edit-distance helpers |
| `evaluate_detection.py` | Detection matching helpers |

Scripts are added as phases are executed.
Keep paths Kaggle-friendly (`/kaggle/input/...`, `/kaggle/working/...`).
