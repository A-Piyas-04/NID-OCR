# configs/detection

PaddleOCR detection training configs for `PP-OCRv5_server_det` will be placed here
after Phase 01 verifies the exact config files for the pinned PaddleOCR version.

Paths inside configs should point at Kaggle dataset locations under `/kaggle/input/...`.
