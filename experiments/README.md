# Experiments

Use this folder only for **lightweight, anonymized summaries** copied back from Kaggle
(for example metric tables or notes). Do not store raw NID images here.

On Kaggle, experiment outputs normally live under:

```text
/kaggle/working/experiments/
/kaggle/working/output/
```

Persist important runs as Kaggle notebook outputs or private dataset versions.
